# HiReFF dependencies — for a full install use:
#   pip install -e .[gsplat,train]
#
# Or for development:
#   pip install -e .[gsplat,train,demo]
#
# See pyproject.toml for the canonical dependency list.
